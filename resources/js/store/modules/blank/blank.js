import {client} from "../../../axios";
import {local, session} from "../../../session";

/* Getters */
import {
} from "./getters";

/* Actions */
import {
} from "./actions";

/* Mutations */
import {
} from "./mutations";

const state = {
};
const getters = {
};
const actions = {
};
const mutations = {
};

export default {
  namespaced: 'true',
  state,
  getters,
  actions,
  mutations
}
